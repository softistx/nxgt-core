---
'@nxgt/openapi-codegen': minor
---

New package: generate TypeScript types and Zod 4 validators from an OpenAPI 3.1 or 3.2 document — one file or split across many, `$ref`s into `node_modules` included — and typed Hono routes bound to it.
