---
---

No version bump: none of the twelve is on the registry yet, so these manifest
corrections belong to the first release itself, `1.0.0`, and not to a patch on
top of it.

What changed: `stx-sdk` is declared again — as a required peer of
`@nxgt/shared-hono` and `@nxgt/shared-graphql` — now that it is published to the
public npm registry; `license` and the `softistx` repository URL are filled in
across the twelve.
