{
	"name": "nxgt-api",
	"description": "Building a Bun + Hono API: a folder per subject rather than per layer, the environment declared in bun.d.ts and parsed once with zod, a Bun.serve entrypoint that binds the parsed port, and an OpenAPI registry each module registers into.",
	"version": "1.0.0",
	"author": {
		"name": "softistx"
	}
}
