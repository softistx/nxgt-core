{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"global": {
			"description": "Global configuration applied across all rules in this document.",
			"type": "object",
			"properties": {
				"basePath": {
					"description": "Prefix prepended to every REST path pattern before matching, e.g. \"/api\".",
					"type": "string"
				},
				"rateLimit": {
					"description": "Default rate limit applied unless a rule declares its own `rateLimit` override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
					"type": "object",
					"properties": {
						"windowMs": {
							"type": "number",
							"description": "Rate-limit window size, in milliseconds."
						},
						"limit": {
							"type": "number",
							"description": "Maximum number of requests allowed per window."
						}
					},
					"required": ["windowMs", "limit"],
					"additionalProperties": false
				},
				"cors": {
					"description": "Default CORS policy applied unless a rule declares its own `cors` override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
					"type": "object",
					"properties": {
						"origins": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Allowed CORS origins."
						},
						"methods": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Allowed CORS HTTP methods."
						},
						"allowedHeaders": {
							"type": "array",
							"items": {
								"type": "string"
							},
							"description": "Allowed CORS request headers."
						}
					},
					"required": ["origins", "methods", "allowedHeaders"],
					"additionalProperties": false
				},
				"providers": {
					"description": "Reserved for future use — not currently enforced by the policy evaluators in this package. Intended to dynamically grant authorities based on claims.",
					"type": "array",
					"items": {
						"type": "object",
						"properties": {
							"when": {
								"type": "string",
								"description": "JavaScript expression evaluated against `claims`; truthy activates this provider."
							},
							"authorities": {
								"type": "array",
								"items": {
									"type": "string"
								},
								"description": "Authorities granted to the caller when `when` evaluates truthily."
							}
						},
						"required": ["when", "authorities"],
						"additionalProperties": false
					}
				}
			},
			"additionalProperties": false
		},
		"rest": {
			"type": "object",
			"propertyNames": {
				"type": "string",
				"description": "Path pattern in path-to-regexp syntax, e.g. \"/users/:id\"."
			},
			"additionalProperties": {
				"type": "object",
				"properties": {
					"GET": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"POST": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"PUT": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"PATCH": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"DELETE": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"HEAD": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"OPTIONS": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"CONNECT": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"TRACE": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					},
					"QUERY": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					}
				},
				"additionalProperties": false,
				"description": "Rule entries keyed by HTTP method for this path pattern."
			},
			"description": "REST authorization rules, keyed by path pattern then HTTP method. The first path pattern that matches the request path (in document order) is used."
		},
		"graphql": {
			"type": "object",
			"properties": {
				"Query": {
					"type": "object",
					"propertyNames": {
						"type": "string",
						"description": "Field name on this GraphQL type, e.g. \"createUser\"."
					},
					"additionalProperties": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					}
				},
				"Mutation": {
					"type": "object",
					"propertyNames": {
						"type": "string",
						"description": "Field name on this GraphQL type, e.g. \"createUser\"."
					},
					"additionalProperties": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					}
				},
				"Subscription": {
					"type": "object",
					"propertyNames": {
						"type": "string",
						"description": "Field name on this GraphQL type, e.g. \"createUser\"."
					},
					"additionalProperties": {
						"type": "object",
						"properties": {
							"authorities": {
								"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
								"anyOf": [
									{
										"type": "array",
										"items": {
											"type": "array",
											"items": {
												"type": "string"
											}
										}
									},
									{
										"type": "null"
									}
								]
							},
							"expression": {
								"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
								"type": "object",
								"properties": {
									"value": {
										"type": "string",
										"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
										"examples": [
											"req.body.username.toLowerCase() !== 'admin'",
											"req.method === 'GET' || claims.roles?.includes('ADMIN')",
											"req.params.id === claims.sub",
											"args.input.username.toLowerCase() !== 'admin'",
											"source.id === claims.sub",
											"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
										]
									},
									"message": {
										"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
										"type": "string"
									}
								},
								"required": ["value"],
								"additionalProperties": false
							},
							"authenticated": {
								"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"public": {
								"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
								"anyOf": [
									{
										"type": "boolean"
									},
									{
										"type": "null"
									}
								]
							},
							"cors": {
								"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"origins": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS origins."
									},
									"methods": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS HTTP methods."
									},
									"allowedHeaders": {
										"type": "array",
										"items": {
											"type": "string"
										},
										"description": "Allowed CORS request headers."
									}
								},
								"required": ["origins", "methods", "allowedHeaders"],
								"additionalProperties": false
							},
							"rateLimit": {
								"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
								"type": "object",
								"properties": {
									"windowMs": {
										"type": "number",
										"description": "Rate-limit window size, in milliseconds."
									},
									"limit": {
										"type": "number",
										"description": "Maximum number of requests allowed per window."
									}
								},
								"required": ["windowMs", "limit"],
								"additionalProperties": false
							}
						},
						"additionalProperties": false
					}
				}
			},
			"additionalProperties": {
				"type": "object",
				"propertyNames": {
					"type": "string",
					"description": "Field name on this GraphQL type, e.g. \"createUser\"."
				},
				"additionalProperties": {
					"type": "object",
					"properties": {
						"authorities": {
							"description": "Authority groups checked against the union of claims.authorities, claims.roles, and space-separated claims.scope tokens. Outer array is AND (every group must be satisfied), inner array is OR (at least one authority in the group must match). Omit, or use an empty/null value, to require only that the caller is authenticated. Example: [[\"ADMIN\", \"users:read\"]] means ADMIN OR users:read; [[\"ADMIN\"], [\"users:read\"]] means ADMIN AND users:read.",
							"anyOf": [
								{
									"type": "array",
									"items": {
										"type": "array",
										"items": {
											"type": "string"
										}
									}
								},
								{
									"type": "null"
								}
							]
						},
						"expression": {
							"description": "Optional JavaScript expression check evaluated in addition to (and after) the authorities check.",
							"type": "object",
							"properties": {
								"value": {
									"type": "string",
									"description": "JavaScript expression source evaluated at request time; must return a truthy value for the rule to allow the request. REST rules see `claims` and `req` (body, params, query, headers, cookies) in scope; GraphQL rules see `claims`, `args`, `source` (the resolver's parent/source value — useful for ownership checks like `source.id === claims.sub`), and `info` (the GraphQLResolveInfo). Example: \"args.input.username.toLowerCase() !== 'admin'\".",
									"examples": [
										"req.body.username.toLowerCase() !== 'admin'",
										"req.method === 'GET' || claims.roles?.includes('ADMIN')",
										"req.params.id === claims.sub",
										"args.input.username.toLowerCase() !== 'admin'",
										"source.id === claims.sub",
										"info.fieldName !== 'ssn' || claims.roles?.includes('ADMIN')"
									]
								},
								"message": {
									"description": "Custom error message surfaced when the expression returns falsy. Defaults to \"Expression check failed\".",
									"type": "string"
								}
							},
							"required": ["value"],
							"additionalProperties": false
						},
						"authenticated": {
							"description": "Declares that this route needs a signed-in caller and no particular authority. Documentary — every matched rule already requires one — but far clearer than an empty `authorities` list or an expression on `claims.sub`. Cannot be combined with `public: true`.",
							"anyOf": [
								{
									"type": "boolean"
								},
								{
									"type": "null"
								}
							]
						},
						"public": {
							"description": "Lets anonymous callers reach this rule instead of being answered 401. Only for routes that carry their own credential (e.g. a share-link token the service validates). Cannot be combined with `authenticated: true`.",
							"anyOf": [
								{
									"type": "boolean"
								},
								{
									"type": "null"
								}
							]
						},
						"cors": {
							"description": "Per-rule CORS override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
							"type": "object",
							"properties": {
								"origins": {
									"type": "array",
									"items": {
										"type": "string"
									},
									"description": "Allowed CORS origins."
								},
								"methods": {
									"type": "array",
									"items": {
										"type": "string"
									},
									"description": "Allowed CORS HTTP methods."
								},
								"allowedHeaders": {
									"type": "array",
									"items": {
										"type": "string"
									},
									"description": "Allowed CORS request headers."
								}
							},
							"required": ["origins", "methods", "allowedHeaders"],
							"additionalProperties": false
						},
						"rateLimit": {
							"description": "Per-rule rate-limit override. Reserved for future use — not currently enforced by the policy evaluators in this package.",
							"type": "object",
							"properties": {
								"windowMs": {
									"type": "number",
									"description": "Rate-limit window size, in milliseconds."
								},
								"limit": {
									"type": "number",
									"description": "Maximum number of requests allowed per window."
								}
							},
							"required": ["windowMs", "limit"],
							"additionalProperties": false
						}
					},
					"additionalProperties": false
				}
			},
			"description": "GraphQL authorization rules, keyed by type name then field name. `Query`/`Mutation`/`Subscription` cover root operation fields; any other type name (e.g. \"User\") targets fields on that type wherever it appears in the schema."
		}
	},
	"additionalProperties": false,
	"description": "Authorization policy document consumed by @nxgt/security/policy — defines REST and/or GraphQL access rules and global configuration."
}
